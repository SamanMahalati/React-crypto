import React from 'react';

//components
import Landing from './Landing';
import HomeContent from './HomeContent';

const Home = () => {
    return (
        <main>
            <Landing/>
            <HomeContent/>
        </main>
    );
};

export default Home;